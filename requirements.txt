# --- Backend (FastAPI) ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5
aiofiles>=23.2

# --- Image / Geo ---
opencv-python>=4.8
pillow>=10.0
numpy>=1.26
rasterio>=1.3        # GeoTIFF reading + affine transforms
pyproj>=3.6          # CRS transformations (WGS84 ↔ UTM)
shapely>=2.0         # geometry ops for export

# --- Models ---
ultralytics>=8.1     # YOLOv8-seg
torch>=2.1           # PyTorch
torchvision>=0.16
deepforest>=1.4      # pre-trained tree detection (Branch A)

# --- Ensemble / post-processing ---
ensemble-boxes>=1.0  # Weighted Box Fusion implementation

# --- SAM2 (DeepForest post-processor) ---
sam2>=1.0

# --- ML utilities ---
pandas>=2.1
matplotlib>=3.8
tqdm>=4.66
pyyaml>=6.0
pycocotools>=2.0     # COCO mAP eval (Mask R-CNN)
albumentations>=1.4  # train-time augmentations (Mask R-CNN)
